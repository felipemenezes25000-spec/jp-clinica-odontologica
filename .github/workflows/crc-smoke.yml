name: CRC Smoke

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: crc-smoke-${{ github.ref }}
  cancel-in-progress: true

jobs:
  smoke:
    name: typecheck, tests and build
    runs-on: ubuntu-latest
    timeout-minutes: 12

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      # O lockfile legado ainda não passa `npm ci` (falta lru-cache@11.5.2).
      # O smoke precisa validar o produto, não falhar antes por essa dívida.
      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Typecheck
        run: npm run typecheck

      - name: Tests
        run: npm run test

      - name: Build
        run: npm run build
