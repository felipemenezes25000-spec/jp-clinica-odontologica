# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Python (scripts/gerar-marca.py)
__pycache__/
*.pyc

# Portal de RH: dados gravados em disco (candidaturas e currículos enviados)
# e segredos locais. Nada disso pode ir para o repositório — .env.example fica
# versionado como referência.
.data/
.env
.env.local

.vercel
.senha-painel.txt
.senha-painel.tmp
.segredo-sessao.tmp

# Skills de agente instalados por `npx skills`.
# O conteudo real fica em .agents/ e e versionado, junto com skills-lock.json.
# Ja .claude/skills/ e so um symlink de caminho ABSOLUTO que o instalador cria
# nesta maquina: com core.symlinks=false o git o gravaria como arquivo de texto,
# e o Claude Code acharia um arquivo onde espera uma pasta. Recriar com
# `npx skills add` e mais barato que versionar um link quebrado.
.claude/skills/
playwright-report/
test-results/
.output/
